import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'app-layout-login',
  imports: [],
  template: './layout-loginComponent.html',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class LayoutLoginComponent {}
